Link para donwload do Git: https://git-scm.com/downloads
O Git Bash é um terminal extendido para otimizar o uso do Git.

Como enviar atualização da máquina local para o repositor remoto?
Comandos:
git add . (envia todos os arquivos)
git commit -m "colocar comentário".
git push origin main (comando que envia todos os commites locais.